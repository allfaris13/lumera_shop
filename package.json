{
    "name": "lumera-shop",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "build": "cd be && npm install && npm run build && npx prisma migrate deploy && cd ../fe && npm install && npx prisma generate && npm run build",
        "start": "concurrently \"cd be && PORT=5000 npm start\" \"cd fe && npm start\""
    },
    "dependencies": {
        "concurrently": "^9.2.1",
        "cross-env": "^10.1.0"
    }
}